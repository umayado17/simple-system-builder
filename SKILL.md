---
name: simple-system-builder
description: 要求整理・要件定義・技術的検討が概ね済んだシステム開発を、Chat上からGitHubを正本として設計、実装、テスト、CI/CD、デバッグ、デプロイまで継続的に進める。目的を満たす最小十分なアーキテクチャを最優先し、帳票、computer use、外部連携など不可避の複雑さを境界層へ隔離する。プロトタイプは単純に作りながら、定数、設定、テンプレート、アダプター、Migrationなど低コストな拡張点だけを先に置き、将来のエンタープライズ化を妨げない。ユーザーが「この要件で開発を開始」「Skillを発動」「GitHubに展開して進めて」「既存リポジトリの続きを自律的に進めて」など、探索から構築へ移る意思を示したときに使う。要求探索や技術相談だけの段階では使わない。
---

# Simple System Builder

## 最上位の使命

目的を満たす最小十分なシステムを作る。技術的な見栄えや将来の可能性だけを理由に複雑さを追加しない。

常に次を守る。

1. 現在の目的と受入条件を満たす、最も単純な構成から始める。
2. 一つで済むものを分けず、運用対象を増やさない。
3. 不可避の複雑さは業務の中心へ混ぜず、明確な境界の外側へ閉じ込める。
4. 将来の機能を先に作らない。将来変わりそうな場所を安く変更できる継ぎ目だけを置く。
5. 設計、実装、テスト、文書、CI/CDをGitHub上で同期し、GitHubを開発状態の正本にする。
6. 重要でない技術判断は自律的に行う。人には目的、業務上の意味、高影響・不可逆な判断だけを求める。

開始時と主要な設計変更時に `references/principles.md` を読む。

## 実行の全体像

1. 会話から目的、利用者、主要ユースケース、受入条件、制約、技術的成立性を抽出する。
2. 探索から構築へ移れるかを判定する。
3. GitHubリポジトリを作成または特定し、プロジェクト状態を初期化する。
4. データモデルとUXを往復しながら基準線を固める。
5. 実行基盤と権限を往復しながら最小権限の基準線を固める。
6. 最小十分アーキテクチャを決め、最小の縦切り機能から実装する。
7. テスト、CI、ローカルまたはstaging実行、デバッグ、文書更新を自動で繰り返す。
8. 高影響な判断だけをユーザーへ戻し、それ以外は継続する。
9. デプロイ後もGitHub Actionsとログを使い、修正と再検証を続ける。

詳細な進行規則は `references/workflow.md` を読む。

## 発動時の判定

次が概ね分かっていれば開始する。完全である必要はない。

- 何を解決するか
- 主な利用者と利用場面
- 主要ユースケース
- 対象範囲と主な対象外
- 受入条件
- 重要なデータと機密性
- 外部システムとの境界
- 実行環境の候補
- 技術的に実現可能であること

軽微な不足は合理的な仮定として記録し、止まらず進める。目的、主要業務フロー、技術的成立性が曖昧な場合だけ、質問をまとめて通常のChatへ戻す。

## アーキテクチャの決め方

新しい技術、サービス、プロセス、抽象化を追加する前に、次を確認する。

- 現在の要件に直接必要か。
- もっと単純な代替がないか。
- 既存の一つの構成で処理できないか。
- 複雑さを境界モジュールへ隔離できないか。
- 障害時に誰が理解し、直し、運用するか。
- 追加コストに見合う検証可能な利益があるか。

説明できなければ追加しない。単一のデプロイ単位、単一の主要データストア、少数の外部サービスを初期値とする。マイクロサービス、イベント基盤、複数DB、独自フレームワーク、コンテナオーケストレーションなどは、現在の要件で必要性が確認できた場合だけ採用する。

各アーキテクチャ文書には、短い「なぜこれが最小十分か」を必ず残す。

## 複雑さを閉じ込める

帳票、computer use、外部API、ストレージ、認証、UI、Chat/MCPなど、変更や失敗が起きやすい技術は、業務の中心から分離する。

```text
利用者・Chat・UI
        ↓
アプリケーションの用件
        ↓
単純な業務ルールと標準データ
        ↓
境界アダプター
        ↓
帳票 / computer use / 外部API / DB / クラウド
```

- 業務の中心はファイル形式、画面座標、外部API固有の都合を知らないようにする。
- 境界側で変換、再試行、タイムアウト、ログ、エラー翻訳を扱う。
- 最初は同一アプリ内の薄いモジュールでよい。別サービスやワーカーへ分割するのは、隔離、性能、信頼性上の必要が生じてからにする。
- 汎用プラグイン基盤を先に作らない。変更可能性が高い境界にだけ、小さなインターフェースを置く。

帳票またはcomputer useを含む場合は `references/complexity-boundaries.md` を読む。

## 単純なプロトタイプと将来の拡張

現在必要な機能だけを実装する。ただし、変更可能性が高く、低コストで備えられる箇所には継ぎ目を置く。

優先する継ぎ目は次のとおり。

- コード中に散らばる値を定数、型付き列挙、設定へ寄せる。
- 環境差を環境変数または設定ファイルへ出す。
- 接続先、認証方式、保存先、外部サービス、帳票レンダラー、computer use実装を境界へ寄せる。
- 帳票文言とレイアウトをテンプレート化し、版を管理する。
- DB変更をMigrationとして管理する。
- 安定した識別子と明確なデータ意味を使う。
- 交換対象の境界に契約テストを置く。

将来の仕組み自体は作らない。拡張点には、現在または予想される近い将来の変更理由が必要である。抽象化のための抽象化を避ける。

アーキテクチャ決定時と大きな変更時は `references/prototype-enterprise.md` を読む。

## 人との協働と自律実行

次はユーザーと双方向で固める。

- データモデルとUXの相互関係
- 現場帳票の内容、意味、出力・印刷上の要件
- computer useで許容できる操作、失敗時の扱い、手動への戻し方
- クラウドまたはローカル実行環境と権限の関係

次は原則として自律的に進める。

- 詳細設計
- コードとプロンプトの実装
- テスト作成と実行
- 文書更新
- CIとGitHub Actions
- ローカルまたはstagingへのデプロイ
- ログ解析、バグ修正、再実行
- 承認済み設計内のリファクタリング

次の場合だけ明示的な判断を求める。

- 目的、主要要件、業務上の意味を変える必要がある。
- 個人情報、機密情報、公開範囲、権限境界が変わる。
- 有料サービス、契約、重大な継続費用が増える。
- 本番データの破壊、復元困難な変更、強い権限付与が必要である。
- 単純さと重要要件が両立せず、優先順位を決める必要がある。
- 安全に試せる単純な選択肢がなく、確信を持てない。

質問はまとめ、推奨案と理由を平易に示す。判断待ちでも独立した作業は続ける。

## GitHubを正本にする

既存リポジトリがあれば、その構成と方針を尊重して続行する。新規の場合は、利用可能なGitHub App、connector、MCP、または認証済みCLIを使う。リポジトリが無い、または書き込みできない場合は、まず `references/tooling.md` の「初回セットアップ」に従い、リポジトリの手動作成とコネクタの書き込み許可設定を案内する。案内後も書き込みできない場合に限り、ローカルに完全なGitHub-ready構成を作り、必要な手動操作を一つにまとめて示す。できない操作を実行したふりはしない。

新規または状態管理がないプロジェクトでは、次を実行する。

```bash
python scripts/scaffold_project.py --root <project-root> --name <project-name> --purpose "<purpose>"
```

続行時は `.chatdev/project-state.json`、主要文書、直近のCI結果を先に読む。詳細は `references/repository-contract.md` を読む。

GitHubでは次を守る。

- 文書を増やしすぎず、意思決定と保守に必要なものだけを置く。
- 小さく意味のある変更単位で記録する。
- コード、テスト、設計文書、状態を同じ変更で同期する。
- CIは現在の技術スタックに必要な最小構成にする。
- Secretをコードやログへ書かない。
- 本番公開、権限拡大、破壊的操作はプロジェクト方針に従って停止する。

GitHubやクラウドの具体的な操作規則は `references/tooling.md` を読む。

## 実装ループ

一度に全体を作らず、利用者に価値が通る最小の縦切り単位で進める。

1. 受入条件を一つ選ぶ。
2. 必要なデータ、UX、境界を確認する。
3. 最小の実装とテストを作る。
4. ローカルまたはstagingで実行する。
5. CIとログで検証する。
6. 失敗を修正して再実行する。
7. 文書とプロジェクト状態を更新する。
8. 次の受入条件へ進む。

同じ問題を安全な方法で解消できない、要件変更が必要、権限追加が必要、または単純さを失う場合だけ人へ戻す。

状態更新には次を使える。

```bash
python scripts/update_state.py --root <project-root> --phase implementation --current-task "<task>" --next-action "<next>"
```

リポジトリ構成の確認には次を使える。

```bash
python scripts/validate_project.py --root <project-root>
```

## 完了条件

完了とは、コードが生成されたことではない。少なくとも次を満たす。

- 目的と主要受入条件を満たす。
- アーキテクチャが最小十分で、追加した複雑さを説明できる。
- 帳票、computer use、外部依存の複雑さが境界へ隔離されている。
- 将来変わりそうな値と接続が、過剰設計なしに変更可能になっている。
- 自動テストとCIが通る。
- 実行・デプロイ・復旧方法が短く説明できる。
- GitHub上の文書と状態が実装に一致している。
- 未実装のエンタープライズ項目は、実装ではなく移行条件と方法として記録されている。
